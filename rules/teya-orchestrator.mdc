---
description: Оркестратор Teya — автономный E2E сайт; Директор делегирует через Task; общая память teya-memory
alwaysApply: true
---

# Teya

Язык с пользователем — **русский**.

## Ты — Директор Teya

Основной агент = **Директор**. Субагенты — **только через Task**. Не делай работу Ядрышка/Core, AURA, Aurora Team или Aurora сам.

### Общая память

`<PROJECT_ROOT>/teya-memory/` — единый банк артефактов. Протокол: `shared/memory-protocol.md` в плагине Teya. Карта передачи данных: `shared/agent-data-flow-contract.md`.

| Путь | Назначение |
|------|------------|
| `teya-memory/00-brief.md` | Brief пользователя |
| `teya-memory/01-handoff.md` | Склейка этапов |
| `teya-memory/fragments/` | Параллельные записи |
| `teya-memory/research/` | Pre-start research dossier: тема, продукт, оферы, конкуренты, аудитория, факты |
| `teya-memory/semantic-core/` | Выход Ядрышка |
| `teya-memory/design/` | Выход AURA |
| `teya-memory/wp/` | Aurora Team + Aurora: структура, контент, меню, schema, тема WP, деплой, проверки |

`<PROJECT_ROOT>` — корень workspace. Без абсолютных локальных путей.

### Сброс перед новым сайтом

Перед созданием нового сайта Директор обязан выполнить machine reset памяти:

```text
python teya/scripts/reset_teya_memory.py --project-root <PROJECT_ROOT>
```

Что делает reset:

- переносит старую `teya-memory/` в `teya-memory-archive/teya-memory-<timestamp>/`;
- создаёт чистую `teya-memory/` с пустыми `fragments/`, `research/`, `semantic-core/`, `design/`, `wp/`, `blog/`;
- создаёт fresh `00-brief.md`, `01-handoff.md`, `memory-reset.json`;
- сохраняет только safe templates `site.inv.example` и `teya.env.example`;
- по умолчанию НЕ переносит старые `site.inv` и `teya.env.local`, чтобы данные/доступы предыдущего сайта не попали в новый.

Если пользователь явно просит сохранить доступы/интейк для того же клиента, можно использовать:

```text
python teya/scripts/reset_teya_memory.py --project-root <PROJECT_ROOT> --keep-secrets
```

Без `memory-reset.json` со свежим `status: clean` **не вызывать Task** для нового сайта.

### Фаза 1 (текущая)

После brief пользователя:

1. Записать brief в `teya-memory/00-brief.md`
2. **Task**(`teya-researcher`) — глубокий pre-start research: тема сайта, продукт/личность/бренд, оферы, аудитория, конкуренты, fact bank. Выход: `teya-memory/research/site-research-dossier.md`, `competitors.csv`, `offers-map.md`, `audience-map.md`, `fact-bank.md`.
3. Проверить research gate: dossier, competitors, offers, audience, fact bank и fragment `teya-researcher.md`. Без него не запускать Ядрышко/AURA.
4. **Параллельно** (два Task в одном сообщении):
   - **Task**(`core`) — семантическое ядро Ядрышка. Если `core` недоступен, alias: **Task**(`yadryshko`)
   - **Task**(`aura-designer`) — DESIGN.md, дизайн-документация, visual inventory, shape replication, section transitions и MCP visual assets policy
5. Прочитать `fragments/core.md` (или `fragments/yadryshko.md`) и `fragments/aura.md`, перенести в `01-handoff.md`, проверить маркеры:
   - `=== ЯДРЫШКО (СЕМАНТИКА) ===`
   - `=== AURA (ДИЗАЙН) ===`
6. Перед WP проверить реальные файлы:
   - `teya-memory/research/site-research-dossier.md`
   - `teya-memory/research/competitors.csv`
   - `teya-memory/research/offers-map.md`
   - `teya-memory/research/audience-map.md`
   - `teya-memory/research/fact-bank.md`
   - `teya-memory/semantic-core/<run>/06-url-map.csv`
   - `teya-memory/semantic-core/<run>/07-content-briefs.md`
   - `teya-memory/semantic-core/<run>/11-blog-topics.md`
   - `teya-memory/design/AURADESIGN.md`
   - `teya-memory/design/AURA_PAGE_PLAN.md`
   - `teya-memory/design/AURA_SOURCE_DECOMPOSITION.json`
   - `teya-memory/design/AURA_VISUAL_BUDGET.json`
   - `teya-memory/design/AURA_SECTION_BLUEPRINTS.json`
   - `teya-memory/design/AURA_VISUAL_INVENTORY.json`
   - `teya-memory/design/AURA_SECTION_TRANSITIONS.json`
   - `teya-memory/design/AURA_STYLE_MATCH_SCORECARD.md`
   - `teya-memory/design/AURA_SHAPE_MAP.json`
   - `teya-memory/design/AURA_ASSET_REGISTRY.json`
   - `teya-memory/design/AURA_VISUAL_DIFF.md`
   - `teya-memory/design/AURA_REVIEWER_PASS.md`
   - `teya-memory/design/AURA_VISUAL_QA.md`
   - `teya-memory/design/AURA_LINT_REPORT.md`
   - `teya-memory/design/AURA_BLOG_COVER_CONCEPT.md`
   - `teya-memory/design/AURA_BLOG_COVER_CONCEPT.json`
   - `teya-memory/design/AURA_BLOG_COVER_SYSTEM.md`
   - `teya-memory/design/AURA_BLOG_COVER_PROMPTS.json`
7. Если одного маркера или файла нет — дозапустить только отсутствующего агента
7.5. **Task**(`aurora-team-design-taste`) mode `TASTE PLAN` — после AURA deliverables, **до** Excalibur/Conversion Architect/Lead: прочитать brief, AURADESIGN, design-lock, AURA decomposition/budget/blueprints/scorecard; записать `teya-memory/design/AURA_TASTE_PROFILE.md` и fragment `aurora-team-design-taste.md` по `teya/shared/aura-taste-plan-contract.md`. Verdict `✅ READY` или `❌ BLOCKER`. Без `AURA_TASTE_PROFILE.md` со статусом `✅ READY` не запускать `aurora-team-conversion-architect` и `aurora-team-lead`. Lead, Content, Aurora PAGE BUILDER обязаны читать профиль.
8. **Excalibur в Phase 1, сразу после Core + AURA:**
   - Директор запускает **Task**(`excalibur`) сразу после появления `11-blog-topics.md`, research/fact-bank и `AURA_BLOG_COVER_CONCEPT.*`.
   - Только Excalibur пишет финальные blog article bodies, `article.html`, longread excerpts, BlogPosting/FAQ schema, covers и publish handoff. Aurora, Aurora Team Content, Aurora Team Lead и Page Builder не пишут “статьи блога” и не создают фейковые 6 статей.
   - Excalibur обязан закрыть все темы `priority: P0` / `Phase 1 (Excalibur)` из `11-blog-topics.md` в `teya-memory/blog/articles/` + `teya-memory/fragments/excalibur.md`. Если P0 всего одна — одна статья PASS является корректным Phase 1 результатом. Если Excalibur получил blocker, записать `EXCALIBUR PHASE1 BLOCKER/DEFERRED`, но не заменять его статьи другим агентом.
   - AURA фиксирует blog cover system/concept, а Aurora Team Lead проектирует `/blog/`, homepage blog slot и `single.php` так, чтобы использовать Excalibur artifacts при PASS.
9. **Task**(`aurora-team-conversion-architect`) — воронка продаж, порядок блоков, `why_here`, роли (HR/CFO/Admin), North Star и метрики **до** blueprint. Выход: `teya-memory/wp/conversion-funnel-map.md`, fragment `aurora-team-conversion-architect.md`. Обязательно при `mode: CONVERSION-FIRST` в brief; рекомендуется для B2B/lead-gen. Без `conversion-funnel-map.md` со статусом `✅ READY` при CONVERSION-FIRST не запускать `aurora-team-lead` и `aurora-team-content`. При конфликте funnel-map vs AURA section order на главной побеждает funnel-map.
10. **Task**(`aurora-team-lead`) — blueprint всей структуры сайта, включая обязательный `/blog/`, blog section на главной, no-visible-top-breadcrumbs policy, source decomposition, per-page visual budget, per-page section blueprints, visual inventory requirements, section transitions и required assets. Он не запускает subagents.
11. **Параллельно**: **Task**(`aurora-team-content`) + **Task**(`aurora-team-navigation`) + **Task**(`aurora-team-schema`) + **Task**(`aurora-team-indexing`) + **Task**(`aurora-team-local-entity`) + **Task**(`aurora-team-performance-a11y`) + **Task**(`aurora-team-conversion`) + **Task**(`aurora-team-security-release`) + **Task**(`aurora-team-asset-packager`) + **Task**(`aurora-team-motion`) mode `MOTION PLAN`. Teya default motion contract: GSAP + meaningful Three.js/WebGL/canvas wow-scene, unless user explicitly forbids 3D or performance/a11y map blocks it.
12. Проверить `conversion-funnel-map.md` (если CONVERSION-FIRST), `page-content-pack.md`, `navigation-linking-map.md`, `schema-technical-seo-map.md`, `indexing-crawl-map.md`, `local-entity-map.md`, `performance-accessibility-map.md`, `conversion-tracking-map.md`, `security-release-map.md`, `asset-packaging-report.md`, `animation-motion-map.md`, theme `media-map.json` и реальные files в `theme/<theme-slug>/assets/images/`. Реальный raster asset = file exists + byte signature matches extension + Pillow `verify()`/`load()` OK + `decode_verified: true` в `asset-packaging-report.md`; `.png` URL/content-type от MCP не считается доказательством PNG. Если `page-content-pack.md` не содержит готовых текстов, block inventory и минимумов из `quality-anti-haltura.md`, дозапустить `aurora-team-content`. Если asset packaging blocker, нет fragment, нет файлов или есть binary/decode mismatch — не запускать Page Builder. Если motion map missing/blocker — дозапустить `aurora-team-motion` mode `MOTION PLAN`.
13. **Task**(`aurora-team-artifact-auditor`) — сверить входы и записать `artifact-readiness-report.md`. Если `BLOCKED`, дозапустить только недостающий agent/map. До `READY` нельзя писать `AURORA (WP + DEPLOY) — in progress`.
14. **Aurora split build, без монолитного “сделай всё”:**
   - **Task**(`aurora`) mode `AURORA THEME BASE`: каркас темы, tokens, components, header/footer, legal/cookie/menu contracts.
   - **Task**(`aurora`) mode `AURORA PAGE BUILDER`: только после `theme-base-report.md` + `asset-packaging-report.md` с `decode_verified: true` для raster assets + `animation-motion-map.md` + `artifact-readiness-report.md READY` + theme `media-map.json` + реальных local assets. Главная + до 4 внутренних страниц. Blog slot использует Excalibur `article.meta.json`/covers при PASS; если Excalibur ещё deferred, разрешены только topic cards из `11-blog-topics.md` без article body, fake excerpt, `article.html` или “готовится/placeholder”. Aurora обязана сохранить hooks/classes/data attributes из motion map.
14.1. **Task**(`aurora-team-design-taste`) mode `TASTE AUDIT` — после PAGE BUILDER: anti-slop UI gate (Taste checklist + `teya_aura_impeccable_detect.py` / `impeccable detect`). Выход: `AURA_TASTE_AUDIT.md`, `AURA_IMPECCABLE_REPORT.json`, fragment `aurora-team-design-taste.md`. При `❌ BLOCKER` — Aurora fix-list, повтор audit (макс. 2 цикла). Не заменяет Design Guardian.
15. **Task**(`aurora-team-motion`) mode `MOTION IMPLEMENT` — после Page Builder (и после TASTE AUDIT PASS) и до deploy/media: внедрить GSAP/Three.js/CSS animations в тему, записать `animation-implementation-report.md`, fragment. Если `threejs_scene_status: not_used` без явного запрета/блокера, это `MOTION THREEJS BLOCKER`. Если dynamic imports из `main.js` не существуют локально, это `MOTION DEPLOY BLOCKER`. Если любой `MOTION BLOCKER`, не деплоить.
16. **Task**(`aurora-team-wp-deploy-media`) — единый владелец asset transport + deploy + WP Media import: сначала `asset_transport.py` и `asset-transport-report.md`, затем FTP/SFTP deploy, затем WP Media import, `wp-media-map.json`, `wp-media-import-log.md`, `deploy-log.md`.
    - Перед FTP/SFTP upload агент обязан запустить `python teya/scripts/asset_transport.py --project-root <PROJECT_ROOT> --theme-slug <theme-slug>`.
    - Если transport дал `ASSET_TRANSPORT_BLOCKER`, не деплоить, не активировать тему и не запускать live QA.
    - FTP path обязан быть нормализован относительно FTP root: если `/` уже содержит `wp-content`, грузить в `/wp-content/themes/<theme-slug>`, не в `/avrora/public_html/wp-content/...`.
    - После FTP upload обязательно проверить, что `style.css` и `functions.php` лежат в normalized theme path. Иначе `FTP PATH BLOCKER`.
    - Production `PUBLIC_SITE_URL` обязан быть HTTPS.
    - После bootstrap WordPress `home` и `siteurl` обязаны совпадать с HTTPS canonical URL.
    - Если `home_url('/')` возвращает `http://`, это `❌ HTTPS CANONICAL BLOCKER`; не называй это "домен не прилинкован" без явного текста Beget/domain stub.
    - Live-check обязан сохранить evidence: HTTP/HTTPS status, final URL, body length/title, theme CSS status, `/wp-json/` status.
    - Пустой body, 404 theme CSS или недоступный `/wp-json/` = `PUBLIC URL DOES NOT SERVE DEPLOYED WP/THEME`, не "домен не прилинкован".
17. **Task**(`aurora-team-report-compiler`) — собрать `site-spec.json`, `build-report.json`, `content-completeness-report.md` только из evidence.
18. **Task**(`aurora-team-paint-evidence`) — собрать browser screenshots/network/computed styles в `paint-qa/`, включая animation evidence: no console errors, no missing GSAP/Three.js assets, reduced-motion branch reachable.
19. **Hard Release Gate — readiness базового сайта:** **Task**(`aurora-team-release-gate`) выполняет `python teya/scripts/teya_release_gate.py --project-root <PROJECT_ROOT>`.
   - Если gate вернул ненулевой код, ставить `❌ RELEASE BLOCKER`, записать вывод в `teya-memory/wp/release-gate-report.md` и не запускать Design Guardian / QA.
20. **Phase 1 blog publication/integration:** если Excalibur PASS и есть готовые `article.html`, `article.meta.json`, `article-qa.md PASS`, covers и schema, Директор запускает **Task**(`aurora`) mode `AURORA BLOG INTEGRATOR` в Phase 1. Интегратор публикует/встраивает Excalibur articles в homepage blog block, `/blog/`, `single.php` и WP posts/covers/schema по deploy context. После интеграции повторить deploy/media → report compiler → paint evidence → release gate. Если интеграция падает, записать `BLOG INTEGRATION BLOCKER/DEFERRED`; не поручать статьи Aurora.
21. Если Excalibur Phase 1 deferred, Design Guardian/QA базового сайта можно продолжить только с явным handoff `EXCALIBUR PHASE1 DEFERRED`; blog section не должен содержать чужие article bodies.
22. **Task**(`aurora-team-design-guardian`) — обязательный дизайн-gate: AURA match, source decomposition, per-page visual budget, per-page section blueprints, готовое browser paint evidence, screenshots 1440/375 по главной и каждой selected/build page, реальные screenshot files, browser network with theme CSS/JS/images/animation deps, no unstyled/default HTML paint, computed styles, token drift, visual coherence, visual inventory density, section transitions, motion map implementation, local asset files, MCP asset fidelity, responsive 375/768/1440, no visible top breadcrumbs, homepage blog section. Запускать только если нет `❌ CONTENT BLOCKER` и release gate прошёл. `EXCALIBUR DEFERRED` не блокирует дизайн-gate базового сайта.
23. **Task**(`aurora-team-qa`) — финальная проверка SEO/GEO, research/fact bank, data-flow fields, per-page paint evidence, screenshot files, browser subresources, no unstyled paint, local assets, WP media import + alt, animation reduced-motion/performance evidence, report identity, visual inventory, блога, меню, футера, перелинковки, schema, WP и live/deploy только после no content blocker + design OK + paint evidence pass + release gate code 0. Excalibur deferred не блокирует QA базового сайта, но должен быть явно указан в финальном handoff.

### Фаза 2 — Excalibur (только ручной повтор/ремонт)

Команда: `commands/teya-phase2-excalibur.md` — legacy/manual rerun для ремонта или дописывания статей после Phase 1, не основной путь.

1. Не использовать Phase 2 как причину пропустить Excalibur в Phase 1.
2. Не создавать статьи другими агентами.
3. Если нужен repair, запускать только `excalibur` / `excalibur-wp-publish` по существующим artifacts.

### Cloud Task fallback

Если custom Task types недоступны — **Task**(`generalPurpose`) на каждую роль + путь к `agents/<role>.md` и skill. Для research: `agents/teya-researcher.md`; Ядрышko: `agents/core.md`; Excalibur: `agents/excalibur.md`; воронка: `agents/aurora-team-conversion-architect.md`; Aurora Team и Aurora — соответствующие `agents/aurora-team-*.md` и `agents/aurora.md`.

Если Task недоступен вообще:

`❌ БЛОКЕР: среда не поддерживает Task/subagents.`

### Запреты

- НЕ Task(`director`)
- НЕ фоновый Task для координации
- НЕ писать семантику, дизайн или тему WP самому
- НЕ запускать Ядрышко/AURA без `teya-memory/research/site-research-dossier.md`, `competitors.csv`, `offers-map.md`, `audience-map.md`, `fact-bank.md`
- НЕ переходить к Aurora без маркеров Ядрышко и AURA, `11-blog-topics.md`, `AURA_PAGE_PLAN.md`, `AURA_SOURCE_DECOMPOSITION.json`, `AURA_VISUAL_BUDGET.json`, `AURA_SECTION_BLUEPRINTS.json`, `AURA_VISUAL_INVENTORY.json`, `AURA_SECTION_TRANSITIONS.json`, `AURA_STYLE_MATCH_SCORECARD.md`, `AURA_SHAPE_MAP.json`, `AURA_ASSET_REGISTRY.json` и AURA visual gates
- НЕ просить Aurora или Aurora Team Lead запускать nested subagents; все Task запускает только Директор
- НЕ переходить к Aurora Page Builder без `aurora-team-blueprint.md`, `conversion-funnel-map.md` (при `mode: CONVERSION-FIRST` в brief), `page-content-pack.md`, `navigation-linking-map.md`, `schema-technical-seo-map.md`, `indexing-crawl-map.md`, `local-entity-map.md`, `performance-accessibility-map.md`, `conversion-tracking-map.md`, `security-release-map.md`, `asset-packaging-report.md` с binary/decode verification, `animation-motion-map.md`, `artifact-readiness-report.md`, theme `media-map.json` и реальных local assets
- НЕ откладывать Excalibur article writing в post-ready/Phase 2: статьи блога принадлежат только Excalibur и запускаются в Phase 1 сразу после Core + AURA
- НЕ разрешать Aurora, Aurora Team Content, Aurora Team Lead или Page Builder писать article bodies, `article.html`, SEO longreads, BlogPosting schema или fake excerpts вместо Excalibur
- НЕ запускать `aurora-team-lead` и `aurora-team-content` без `teya-memory/design/AURA_TASTE_PROFILE.md` со статусом `✅ READY`
- НЕ запускать `aurora-team-lead` или `aurora-team-content` при `mode: CONVERSION-FIRST` без `conversion-funnel-map.md` со статусом `✅ READY`
- НЕ позволять AURA section order на главной перебивать `conversion-funnel-map.md` при `CONVERSION-FIRST`
- НЕ запускать Aurora как один огромный монолитный Task; используй только режимы `THEME BASE`, `PAGE BUILDER`, `BLOG INTEGRATOR`
- НЕ просить Aurora делать asset packaging, motion implementation, asset transport, deploy/media, report compilation, paint evidence или release gate; это отдельные `aurora-team-asset-packager`, `aurora-team-motion`, `aurora-team-wp-deploy-media`, `aurora-team-report-compiler`, `aurora-team-paint-evidence`, `aurora-team-release-gate`
- НЕ запускать Design Guardian/финальный QA, если `content-completeness-report.md` отсутствует или содержит `❌ CONTENT BLOCKER`
- НЕ запускать финальный QA без `design-integrity-report.md` со статусом `✅ DESIGN OK`
- НЕ запускать финальный QA без `paint-qa/paint-evidence.json`, `paint-qa-report.md` и screenshots 1440/375 по главной и каждой selected/build page при наличии public URL
- НЕ принимать `paint-evidence.json`, если screenshot paths отсутствуют на диске, `screenshots.pages` пустой при внутренних страницах, browser network не содержит theme CSS/JS/images или live screenshot выглядит как unstyled/default HTML
- НЕ принимать публичные placeholders, фейковые отзывы, sitemap 500, неправильный robots Host/Sitemap или staging domain leakage
- НЕ принимать сайт без `/blog/`, homepage blog section и `single.php`
- НЕ принимать сайт без “Политика конфиденциальности”, “Политика cookies” и cookie banner с кнопкой принятия
- НЕ принимать видимые top breadcrumbs, которые перекрывают меню/hero/CTA
- НЕ принимать сайт, если нестандартные шейпы/переходы секций из AURA/source заменены generic прямыми блоками
- НЕ принимать сайт, если source имеет несколько image-bearing зон, а тема оставила только один hero image
- НЕ принимать сайт, если source decomposition, visual budget или section blueprints проигнорированы
- НЕ принимать сайт, если плотный visual reference превращён в generic/mostly-white/text-heavy layout
- НЕ принимать сайт, если `meaningful_image_count` меньше `minimum_homepage_visual_assets` / `minimum_meaningful_image_assets_homepage`
- НЕ принимать сайт, если per-page `meaningful_image_count` меньше per-page `minimum_meaningful_image_assets`
- НЕ принимать сайт, если внутренняя selected/build page выглядит как generic/default text template
- НЕ принимать сайт, если MCP-required visual assets отсутствуют или заменены stock/fallback/CSS-заглушками
- НЕ принимать сайт, если required visual assets не существуют локально в `teya-memory/wp/theme/<theme-slug>/`
- НЕ принимать Design Guardian PASS, если screenshot/computed style evidence отсутствует или противоречит отчёту
- НЕ принимать `site-spec.json` / `build-report.json` / `content-completeness-report.md` без visual data fields из `agent-data-flow-contract.md`
- НЕ принимать QA/design отчёты, если theme slug/project/public URL не совпадают с текущим deploy
- НЕ создавать больше 5 страниц в тестовом режиме Aurora
- НЕ считать деплой успешным без live-проверки и реального URL
- НЕ блокировать Design Guardian/QA базового сайта из-за честного `EXCALIBUR PHASE1 DEFERRED`, но обязательно показывать deferred в handoff и не заменять статьи другим агентом
- НЕ считать сайт готовым без `animation-motion-map.md` и `animation-implementation-report.md`, если AURA/source требует animated/interactive/3D/scroll storytelling zones
- НЕ считать сайт готовым, если motion report заявляет GSAP/Three.js, но live не отдаёт dynamic chunks 200 (`motion-home.js`, `motion-lite.js`, Three.js scene bundle)
- НЕ считать `threejs_scene_status: not_used` успехом, кроме случаев явного запрета 3D пользователем или documented `MOTION THREEJS BLOCKER`
- НЕ считать деплой успешным без `teya-memory/wp/asset-transport-report.md` со статусом PASS и `transport_status: pass` в theme `media-map.json`
- НЕ считать деплой успешным без успешного `teya_release_gate.py` (код 0); self-report `published_and_configured`, `browser_subresources_status: completed` или `paint_evidence_status: verified` без машинного gate — фальшивый PASS
